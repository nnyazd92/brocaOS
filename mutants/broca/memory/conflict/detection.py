"""
Conflict detection engine.

Detects contradictions and conflicts between memories using multiple methods:
- Semantic similarity (embedding-based)
- Rule-based pattern matching
- LLM-based analysis (optional)
"""

from __future__ import annotations

import re
import json
import logging
from typing import List, Optional, Dict, Any, TYPE_CHECKING
from datetime import datetime, timezone, timedelta

from .. import MemoryRecord
from .models import Conflict

if TYPE_CHECKING:
    from ...reasoning.llm_pattern_matcher import LLMPatternMatcher

logger = logging.getLogger(__name__)


# Legacy regex patterns (kept for fallback only)
CONTRADICTION_PATTERNS = [
    # Boolean contradictions
    (r'\b(prefers?|likes?|loves?|enjoys?)\b', r'\b(hates?|dislikes?|loathes?|despises?)\b'),
    (r'\b(always|all|every)\b', r'\b(never|none|no)\b'),
    (r'\b(yes|true|correct)\b', r'\b(no|false|incorrect|wrong)\b'),
    # Temporal contradictions
    (r'\b(yesterday|past|ago|was)\b', r'\b(tomorrow|future|will be|is)\b'),
    (r'\b(always|all the time)\b', r'\b(sometimes|occasionally|rarely)\b'),
    # Numerical contradictions (will be handled separately)
]
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class ConflictDetector:
    """
    Detects conflicts between memories using multiple detection methods.
    
    Uses semantic similarity, rule-based patterns, and optional LLM analysis
    to identify contradictions and conflicts.
    """
    
    def xǁConflictDetectorǁ__init____mutmut_orig(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_1(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 1.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_2(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 1.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_3(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = None
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_4(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = None
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_5(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = None
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_6(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = None
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_7(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = None
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_8(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None or llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_9(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is not None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_10(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_11(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = None
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_12(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(None, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_13(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, None, 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_14(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', None)
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_15(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr('llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_16(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_17(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', )
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_18(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'XXllm_pattern_matching_modelXX', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_19(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'LLM_PATTERN_MATCHING_MODEL', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_20(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'XXgpt-5-nanoXX')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_21(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'GPT-5-NANO')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_22(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = None
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_23(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=None,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_24(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=None
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_25(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_26(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_27(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(None)
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_28(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(None)
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_29(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = ""
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is not None})"
        )
    
    def xǁConflictDetectorǁ__init____mutmut_30(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            None
        )
    
    def xǁConflictDetectorǁ__init____mutmut_31(
        self,
        memory_manager: Optional[Any] = None,
        similarity_threshold: float = 0.85,
        contradiction_threshold: float = 0.7,
        llm_client: Optional[Any] = None,
        pattern_matcher: Optional["LLMPatternMatcher"] = None
    ) -> None:
        """
        Initialize conflict detector.
        
        Args:
            memory_manager: MemoryManager instance for semantic search (optional)
            similarity_threshold: Minimum similarity to consider for conflicts (0.0-1.0)
            contradiction_threshold: Minimum confidence for contradiction (0.0-1.0)
            llm_client: Optional LLM client for advanced analysis
            pattern_matcher: Optional LLMPatternMatcher for semantic pattern matching
        """
        self.memory_manager = memory_manager
        self.similarity_threshold = similarity_threshold
        self.contradiction_threshold = contradiction_threshold
        self.llm_client = llm_client
        self.pattern_matcher = pattern_matcher
        
        # Initialize pattern matcher if not provided but LLM client is available
        if self.pattern_matcher is None and llm_client is not None:
            try:
                from ...reasoning.llm_pattern_matcher import LLMPatternMatcher
                from ...config import config
                
                model = getattr(config.reasoning, 'llm_pattern_matching_model', 'gpt-5-nano')
                self.pattern_matcher = LLMPatternMatcher(
                    llm_client=llm_client,
                    model=model
                )
                logger.info(f"Initialized LLM pattern matcher for conflict detection (model: {model})")
            except Exception as e:
                logger.warning(f"Failed to initialize LLM pattern matcher: {e}. Will use fallback regex patterns.")
                self.pattern_matcher = None
        
        logger.info(
            f"Initialized ConflictDetector "
            f"(similarity_threshold={similarity_threshold}, "
            f"contradiction_threshold={contradiction_threshold}, "
            f"llm_pattern_matching={self.pattern_matcher is None})"
        )
    
    xǁConflictDetectorǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ__init____mutmut_1': xǁConflictDetectorǁ__init____mutmut_1, 
        'xǁConflictDetectorǁ__init____mutmut_2': xǁConflictDetectorǁ__init____mutmut_2, 
        'xǁConflictDetectorǁ__init____mutmut_3': xǁConflictDetectorǁ__init____mutmut_3, 
        'xǁConflictDetectorǁ__init____mutmut_4': xǁConflictDetectorǁ__init____mutmut_4, 
        'xǁConflictDetectorǁ__init____mutmut_5': xǁConflictDetectorǁ__init____mutmut_5, 
        'xǁConflictDetectorǁ__init____mutmut_6': xǁConflictDetectorǁ__init____mutmut_6, 
        'xǁConflictDetectorǁ__init____mutmut_7': xǁConflictDetectorǁ__init____mutmut_7, 
        'xǁConflictDetectorǁ__init____mutmut_8': xǁConflictDetectorǁ__init____mutmut_8, 
        'xǁConflictDetectorǁ__init____mutmut_9': xǁConflictDetectorǁ__init____mutmut_9, 
        'xǁConflictDetectorǁ__init____mutmut_10': xǁConflictDetectorǁ__init____mutmut_10, 
        'xǁConflictDetectorǁ__init____mutmut_11': xǁConflictDetectorǁ__init____mutmut_11, 
        'xǁConflictDetectorǁ__init____mutmut_12': xǁConflictDetectorǁ__init____mutmut_12, 
        'xǁConflictDetectorǁ__init____mutmut_13': xǁConflictDetectorǁ__init____mutmut_13, 
        'xǁConflictDetectorǁ__init____mutmut_14': xǁConflictDetectorǁ__init____mutmut_14, 
        'xǁConflictDetectorǁ__init____mutmut_15': xǁConflictDetectorǁ__init____mutmut_15, 
        'xǁConflictDetectorǁ__init____mutmut_16': xǁConflictDetectorǁ__init____mutmut_16, 
        'xǁConflictDetectorǁ__init____mutmut_17': xǁConflictDetectorǁ__init____mutmut_17, 
        'xǁConflictDetectorǁ__init____mutmut_18': xǁConflictDetectorǁ__init____mutmut_18, 
        'xǁConflictDetectorǁ__init____mutmut_19': xǁConflictDetectorǁ__init____mutmut_19, 
        'xǁConflictDetectorǁ__init____mutmut_20': xǁConflictDetectorǁ__init____mutmut_20, 
        'xǁConflictDetectorǁ__init____mutmut_21': xǁConflictDetectorǁ__init____mutmut_21, 
        'xǁConflictDetectorǁ__init____mutmut_22': xǁConflictDetectorǁ__init____mutmut_22, 
        'xǁConflictDetectorǁ__init____mutmut_23': xǁConflictDetectorǁ__init____mutmut_23, 
        'xǁConflictDetectorǁ__init____mutmut_24': xǁConflictDetectorǁ__init____mutmut_24, 
        'xǁConflictDetectorǁ__init____mutmut_25': xǁConflictDetectorǁ__init____mutmut_25, 
        'xǁConflictDetectorǁ__init____mutmut_26': xǁConflictDetectorǁ__init____mutmut_26, 
        'xǁConflictDetectorǁ__init____mutmut_27': xǁConflictDetectorǁ__init____mutmut_27, 
        'xǁConflictDetectorǁ__init____mutmut_28': xǁConflictDetectorǁ__init____mutmut_28, 
        'xǁConflictDetectorǁ__init____mutmut_29': xǁConflictDetectorǁ__init____mutmut_29, 
        'xǁConflictDetectorǁ__init____mutmut_30': xǁConflictDetectorǁ__init____mutmut_30, 
        'xǁConflictDetectorǁ__init____mutmut_31': xǁConflictDetectorǁ__init____mutmut_31
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁConflictDetectorǁ__init____mutmut_orig)
    xǁConflictDetectorǁ__init____mutmut_orig.__name__ = 'xǁConflictDetectorǁ__init__'
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_orig(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_1(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = None
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_2(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = None
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_3(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id == memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_4(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_5(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = None
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_6(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(None, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_7(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, None)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_8(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_9(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, )
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_10(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(None, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_11(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, None)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_12(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_13(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, )
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_14(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(None)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_15(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = None
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_16(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                None,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_17(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                None,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_18(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=None,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_19(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=None
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_20(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_21(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_22(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_23(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_24(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(None, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_25(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, None)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_26(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_27(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, )
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_28(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_29(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    None
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_30(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) and (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_31(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id or c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_32(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id != memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_33(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id != existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_34(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id or c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_35(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id != existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_36(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id != memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_37(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id or c.memory2.id
                ):
                    conflicts.append(rule_conflict)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_conflicts__mutmut_38(
        self,
        memory: MemoryRecord,
        existing_memories: List[MemoryRecord]
    ) -> List[Conflict]:
        """
        Detect conflicts between a new memory and existing memories.
        
        Args:
            memory: New memory to check
            existing_memories: List of existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        conflicts: List[Conflict] = []
        
        # Filter out the memory itself from existing memories
        existing = [m for m in existing_memories if m.id != memory.id]
        
        if not existing:
            return conflicts
        
        # Try semantic detection first (if memory_manager available)
        if self.memory_manager:
            semantic_conflicts = self.detect_semantic_conflicts(memory, existing)
            # Enhance with temporal context
            for conflict in semantic_conflicts:
                self._enhance_conflict_with_temporal_context(conflict, memory)
            conflicts.extend(semantic_conflicts)
        
        # Also check rule-based for all pairs
        for existing_memory in existing:
            rule_conflict = self.detect_rule_based_conflicts(
                memory.text,
                existing_memory.text,
                memory1=memory,
                memory2=existing_memory
            )
            if rule_conflict:
                # Enhance with temporal context
                self._enhance_conflict_with_temporal_context(rule_conflict, memory)
                
                # Check if we already have this conflict from semantic detection
                if not any(
                    (c.memory1.id == memory.id and c.memory2.id == existing_memory.id) or
                    (c.memory1.id == existing_memory.id and c.memory2.id == memory.id)
                    for c in conflicts
                    if c.memory1.id and c.memory2.id
                ):
                    conflicts.append(None)
        
        return conflicts
    
    xǁConflictDetectorǁdetect_conflicts__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁdetect_conflicts__mutmut_1': xǁConflictDetectorǁdetect_conflicts__mutmut_1, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_2': xǁConflictDetectorǁdetect_conflicts__mutmut_2, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_3': xǁConflictDetectorǁdetect_conflicts__mutmut_3, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_4': xǁConflictDetectorǁdetect_conflicts__mutmut_4, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_5': xǁConflictDetectorǁdetect_conflicts__mutmut_5, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_6': xǁConflictDetectorǁdetect_conflicts__mutmut_6, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_7': xǁConflictDetectorǁdetect_conflicts__mutmut_7, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_8': xǁConflictDetectorǁdetect_conflicts__mutmut_8, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_9': xǁConflictDetectorǁdetect_conflicts__mutmut_9, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_10': xǁConflictDetectorǁdetect_conflicts__mutmut_10, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_11': xǁConflictDetectorǁdetect_conflicts__mutmut_11, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_12': xǁConflictDetectorǁdetect_conflicts__mutmut_12, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_13': xǁConflictDetectorǁdetect_conflicts__mutmut_13, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_14': xǁConflictDetectorǁdetect_conflicts__mutmut_14, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_15': xǁConflictDetectorǁdetect_conflicts__mutmut_15, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_16': xǁConflictDetectorǁdetect_conflicts__mutmut_16, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_17': xǁConflictDetectorǁdetect_conflicts__mutmut_17, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_18': xǁConflictDetectorǁdetect_conflicts__mutmut_18, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_19': xǁConflictDetectorǁdetect_conflicts__mutmut_19, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_20': xǁConflictDetectorǁdetect_conflicts__mutmut_20, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_21': xǁConflictDetectorǁdetect_conflicts__mutmut_21, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_22': xǁConflictDetectorǁdetect_conflicts__mutmut_22, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_23': xǁConflictDetectorǁdetect_conflicts__mutmut_23, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_24': xǁConflictDetectorǁdetect_conflicts__mutmut_24, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_25': xǁConflictDetectorǁdetect_conflicts__mutmut_25, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_26': xǁConflictDetectorǁdetect_conflicts__mutmut_26, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_27': xǁConflictDetectorǁdetect_conflicts__mutmut_27, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_28': xǁConflictDetectorǁdetect_conflicts__mutmut_28, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_29': xǁConflictDetectorǁdetect_conflicts__mutmut_29, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_30': xǁConflictDetectorǁdetect_conflicts__mutmut_30, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_31': xǁConflictDetectorǁdetect_conflicts__mutmut_31, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_32': xǁConflictDetectorǁdetect_conflicts__mutmut_32, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_33': xǁConflictDetectorǁdetect_conflicts__mutmut_33, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_34': xǁConflictDetectorǁdetect_conflicts__mutmut_34, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_35': xǁConflictDetectorǁdetect_conflicts__mutmut_35, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_36': xǁConflictDetectorǁdetect_conflicts__mutmut_36, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_37': xǁConflictDetectorǁdetect_conflicts__mutmut_37, 
        'xǁConflictDetectorǁdetect_conflicts__mutmut_38': xǁConflictDetectorǁdetect_conflicts__mutmut_38
    }
    
    def detect_conflicts(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁdetect_conflicts__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁdetect_conflicts__mutmut_mutants"), args, kwargs, self)
        return result 
    
    detect_conflicts.__signature__ = _mutmut_signature(xǁConflictDetectorǁdetect_conflicts__mutmut_orig)
    xǁConflictDetectorǁdetect_conflicts__mutmut_orig.__name__ = 'xǁConflictDetectorǁdetect_conflicts'
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_orig(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_1(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager and not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_2(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_3(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_4(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = None
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_5(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = None
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_6(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=None,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_7(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=None  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_8(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_9(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_10(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=11  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_11(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id != new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_12(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    break
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_13(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace or similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_14(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace != new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_15(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text != new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_16(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    break
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_17(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = None
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_18(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(None, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_19(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, None)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_20(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_21(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, )
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_22(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity > self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_23(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = None
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_24(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        None,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_25(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        None
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_26(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_27(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_28(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score > self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_29(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = ""
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_30(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = None
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_31(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                None,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_32(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                None
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_33(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_34(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_35(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = None
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_36(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "XXcontradictionXX"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_37(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "CONTRADICTION"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_38(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = None
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_39(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = None
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_40(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result or llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_41(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get(None):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_42(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("XXcontradictsXX"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_43(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("CONTRADICTS"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_44(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = None
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_45(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get(None, "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_46(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", None)
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_47(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_48(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", )
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_49(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("XXtypeXX", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_50(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("TYPE", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_51(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "XXcontradictionXX")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_52(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "CONTRADICTION")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_53(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = None
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_54(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(None, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_55(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, None)
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_56(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_57(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, )
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_58(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get(None, 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_59(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", None))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_60(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get(0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_61(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", ))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_62(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("XXconfidenceXX", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_63(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("CONFIDENCE", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_64(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 1.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_65(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = None
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_66(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get(None, evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_67(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", None)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_68(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get(evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_69(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", )
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_70(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("XXexplanationXX", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_71(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("EXPLANATION", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_72(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(None)
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_73(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=None,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_74(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=None,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_75(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=None,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_76(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=None,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_77(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=None,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_78(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy=None
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_79(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_80(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_81(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_82(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_83(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_84(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_85(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="XXrecencyXX"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_86(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="RECENCY"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_87(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(None, exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_88(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=None)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_89(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(exc_info=True)
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_90(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", )
        
        return conflicts
    
    def xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_91(
        self,
        new_memory: MemoryRecord,
        existing_memories: List[MemoryRecord],
    ) -> List[Conflict]:
        """
        Detect conflicts using embedding similarity.
        
        Args:
            new_memory: New memory to check
            existing_memories: Existing memories to check against
            
        Returns:
            List of Conflict objects
        """
        if not self.memory_manager or not existing_memories:
            return []
        
        conflicts: List[Conflict] = []
        
        try:
            # Use memory manager to find similar memories
            # Search for similar memories using the new memory's text
            similar_memories = self.memory_manager.retrieve_memories(
                query=new_memory.text,
                limit=10  # Check top 10 similar
            )
            
            for similar_memory in similar_memories:
                # Skip if it's the same memory
                if similar_memory.id == new_memory.id:
                    continue
                
                # Skip exact duplicates (handled by deduplication)
                if (similar_memory.namespace == new_memory.namespace and
                    similar_memory.text == new_memory.text):
                    continue
                
                # Calculate similarity (using embedding if available)
                similarity = self._calculate_similarity(new_memory, similar_memory)
                
                if similarity >= self.similarity_threshold:
                    # High similarity - check for contradiction
                    contradiction_score = self._check_contradiction(
                        new_memory.text,
                        similar_memory.text
                    )
                    
                    if contradiction_score >= self.contradiction_threshold:
                        # Use LLM if available for better analysis
                        llm_result = None
                        if self.llm_client:
                            llm_result = self.analyze_with_llm(
                                new_memory.text,
                                similar_memory.text
                            )
                        
                        conflict_type = "contradiction"
                        confidence = contradiction_score
                        evidence = f"Semantic similarity: {similarity:.2f}, contradiction score: {contradiction_score:.2f}"
                        
                        if llm_result and llm_result.get("contradicts"):
                            conflict_type = llm_result.get("type", "contradiction")
                            confidence = max(confidence, llm_result.get("confidence", 0.0))
                            evidence = llm_result.get("explanation", evidence)
                        
                        conflicts.append(Conflict(
                            memory1=new_memory,
                            memory2=similar_memory,
                            conflict_type=conflict_type,
                            confidence=confidence,
                            evidence=evidence,
                            resolution_strategy="recency"
                        ))
        
        except Exception as e:
            logger.warning(f"Error in semantic conflict detection: {e}", exc_info=False)
        
        return conflicts
    
    xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_1': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_1, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_2': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_2, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_3': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_3, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_4': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_4, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_5': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_5, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_6': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_6, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_7': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_7, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_8': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_8, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_9': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_9, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_10': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_10, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_11': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_11, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_12': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_12, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_13': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_13, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_14': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_14, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_15': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_15, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_16': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_16, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_17': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_17, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_18': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_18, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_19': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_19, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_20': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_20, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_21': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_21, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_22': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_22, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_23': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_23, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_24': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_24, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_25': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_25, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_26': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_26, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_27': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_27, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_28': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_28, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_29': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_29, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_30': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_30, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_31': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_31, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_32': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_32, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_33': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_33, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_34': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_34, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_35': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_35, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_36': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_36, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_37': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_37, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_38': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_38, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_39': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_39, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_40': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_40, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_41': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_41, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_42': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_42, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_43': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_43, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_44': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_44, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_45': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_45, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_46': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_46, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_47': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_47, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_48': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_48, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_49': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_49, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_50': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_50, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_51': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_51, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_52': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_52, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_53': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_53, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_54': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_54, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_55': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_55, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_56': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_56, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_57': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_57, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_58': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_58, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_59': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_59, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_60': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_60, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_61': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_61, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_62': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_62, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_63': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_63, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_64': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_64, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_65': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_65, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_66': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_66, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_67': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_67, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_68': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_68, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_69': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_69, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_70': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_70, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_71': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_71, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_72': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_72, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_73': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_73, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_74': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_74, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_75': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_75, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_76': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_76, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_77': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_77, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_78': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_78, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_79': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_79, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_80': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_80, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_81': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_81, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_82': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_82, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_83': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_83, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_84': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_84, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_85': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_85, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_86': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_86, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_87': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_87, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_88': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_88, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_89': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_89, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_90': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_90, 
        'xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_91': xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_91
    }
    
    def detect_semantic_conflicts(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_mutants"), args, kwargs, self)
        return result 
    
    detect_semantic_conflicts.__signature__ = _mutmut_signature(xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_orig)
    xǁConflictDetectorǁdetect_semantic_conflicts__mutmut_orig.__name__ = 'xǁConflictDetectorǁdetect_semantic_conflicts'
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_orig(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_1(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = None
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_2(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(None, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_3(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, None, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_4(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, None, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_5(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, None)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_6(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_7(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_8(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_9(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, )
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_10(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_11(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = None
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_12(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "XXtypeXX": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_13(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "TYPE": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_14(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "XXcontradiction_checkXX",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_15(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "CONTRADICTION_CHECK",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_16(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "XXtextXX": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_17(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "TEXT": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_18(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "XXdescriptionXX": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_19(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "DESCRIPTION": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_20(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "XXCheck if this statement contradicts another statementXX"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_21(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_22(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "CHECK IF THIS STATEMENT CONTRADICTS ANOTHER STATEMENT"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_23(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = None
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_24(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "XXtypeXX": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_25(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "TYPE": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_26(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "XXtextXX",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_27(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "TEXT",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_28(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "XXtextXX": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_29(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "TEXT": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_30(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "XXdescriptionXX": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_31(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "DESCRIPTION": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_32(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "XXStatement to check for contradictionXX"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_33(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_34(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "STATEMENT TO CHECK FOR CONTRADICTION"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_35(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = None
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_36(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch(None)
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_37(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = None
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_38(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[1]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_39(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match or confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_40(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence > self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_41(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = None
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_42(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 and MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_43(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace=None, text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_44(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=None, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_45(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=None)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_46(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_47(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_48(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, )
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_49(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="XXtempXX", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_50(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="TEMP", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_51(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=1.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_52(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = None
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_53(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 and MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_54(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace=None, text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_55(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=None, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_56(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=None)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_57(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_58(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_59(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, )
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_60(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="XXtempXX", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_61(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="TEMP", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_62(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=1.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_63(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=None,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_64(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=None,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_65(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type=None,
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_66(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=None,  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_67(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=None,
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_68(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy=None
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_69(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_70(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_71(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_72(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_73(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_74(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_75(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="XXcontradictionXX",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_76(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="CONTRADICTION",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_77(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(None, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_78(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, None),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_79(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_80(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, ),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_81(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 1.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_82(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="XXrecencyXX"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_83(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="RECENCY"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_84(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(None)
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_85(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = None
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_86(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.upper()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_87(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = None
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_88(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.upper()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_89(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = None
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_90(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(None, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_91(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, None)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_92(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_93(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, )
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_94(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = None
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_95(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(None, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_96(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, None)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_97(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_98(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, )
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_99(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 or match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_100(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = None
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_101(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(None, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_102(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, None, match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_103(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), None)
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_104(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_105(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_106(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), )
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_107(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = None
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_108(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(None, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_109(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, None, match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_110(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), None)
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_111(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_112(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_113(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), )
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_114(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(None, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_115(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, None):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_116(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_117(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, ):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_118(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = None
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_119(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 and MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_120(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace=None, text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_121(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=None, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_122(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=None)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_123(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_124(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_125(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, )
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_126(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="XXtempXX", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_127(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="TEMP", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_128(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=1.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_129(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = None
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_130(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 and MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_131(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace=None, text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_132(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=None, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_133(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=None)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_134(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_135(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_136(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, )
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_137(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="XXtempXX", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_138(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="TEMP", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_139(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=1.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_140(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=None,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_141(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=None,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_142(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type=None,
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_143(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=None,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_144(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=None,
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_145(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy=None
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_146(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_147(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_148(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_149(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_150(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_151(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_152(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="XXcontradictionXX",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_153(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="CONTRADICTION",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_154(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=1.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="recency"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_155(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="XXrecencyXX"
                    )
        
        return None
    
    def xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_156(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """
        Detect conflicts using LLM-based semantic pattern matching.
        
        Falls back to regex patterns if LLM matcher is not available.
        
        Args:
            text1: First text to compare
            text2: Second text to compare
            memory1: Optional first memory record
            memory2: Optional second memory record
            
        Returns:
            Conflict object if detected, None otherwise
        """
        # Check for numerical contradictions first (this is still useful)
        num_conflict = self._detect_numerical_contradiction(text1, text2, memory1, memory2)
        if num_conflict:
            return num_conflict
        
        # Use LLM pattern matcher if available
        if self.pattern_matcher is not None:
            try:
                # Create pattern for contradiction detection
                pattern = {
                    "type": "contradiction_check",
                    "text": text1,
                    "description": "Check if this statement contradicts another statement"
                }
                content = {
                    "type": "text",
                    "text": text2,
                    "description": "Statement to check for contradiction"
                }
                
                # Use LLM to detect contradiction
                matches = self.pattern_matcher.match_batch([(pattern, content)])
                if matches:
                    match, confidence = matches[0]
                    if match and confidence >= self.contradiction_threshold:
                        # Use provided memories or create temporary ones
                        mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                        mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                        
                        return Conflict(
                            memory1=mem1,
                            memory2=mem2,
                            conflict_type="contradiction",
                            confidence=min(confidence, 0.95),  # Cap at 0.95
                            evidence=f"LLM-based semantic contradiction detection (confidence: {confidence:.2f})",
                            resolution_strategy="recency"
                        )
            except Exception as e:
                logger.warning(f"Error in LLM pattern matching for conflict detection: {e}. Falling back to regex.")
                # Fall through to regex fallback
        
        # Fallback to regex patterns (backward compatibility)
        text1_lower = text1.lower()
        text2_lower = text2.lower()
        
        for pattern1, pattern2 in CONTRADICTION_PATTERNS:
            match1 = re.search(pattern1, text1_lower)
            match2 = re.search(pattern2, text2_lower)
            
            if match1 and match2:
                # Check if they're talking about the same thing (simple heuristic)
                # Extract context around matches
                context1 = self._extract_context(text1_lower, match1.start(), match1.end())
                context2 = self._extract_context(text2_lower, match2.start(), match2.end())
                
                # Simple overlap check
                if self._contexts_overlap(context1, context2):
                    # Use provided memories or create temporary ones
                    mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                    mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                    
                    return Conflict(
                        memory1=mem1,
                        memory2=mem2,
                        conflict_type="contradiction",
                        confidence=0.75,  # Medium confidence for rule-based
                        evidence=f"Regex fallback: {pattern1} vs {pattern2}",
                        resolution_strategy="RECENCY"
                    )
        
        return None
    
    xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_1': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_1, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_2': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_2, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_3': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_3, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_4': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_4, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_5': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_5, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_6': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_6, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_7': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_7, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_8': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_8, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_9': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_9, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_10': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_10, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_11': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_11, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_12': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_12, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_13': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_13, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_14': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_14, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_15': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_15, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_16': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_16, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_17': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_17, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_18': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_18, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_19': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_19, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_20': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_20, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_21': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_21, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_22': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_22, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_23': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_23, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_24': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_24, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_25': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_25, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_26': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_26, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_27': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_27, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_28': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_28, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_29': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_29, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_30': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_30, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_31': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_31, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_32': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_32, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_33': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_33, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_34': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_34, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_35': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_35, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_36': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_36, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_37': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_37, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_38': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_38, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_39': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_39, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_40': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_40, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_41': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_41, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_42': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_42, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_43': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_43, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_44': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_44, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_45': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_45, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_46': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_46, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_47': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_47, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_48': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_48, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_49': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_49, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_50': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_50, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_51': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_51, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_52': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_52, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_53': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_53, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_54': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_54, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_55': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_55, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_56': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_56, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_57': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_57, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_58': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_58, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_59': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_59, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_60': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_60, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_61': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_61, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_62': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_62, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_63': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_63, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_64': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_64, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_65': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_65, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_66': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_66, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_67': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_67, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_68': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_68, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_69': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_69, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_70': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_70, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_71': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_71, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_72': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_72, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_73': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_73, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_74': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_74, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_75': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_75, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_76': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_76, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_77': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_77, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_78': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_78, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_79': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_79, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_80': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_80, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_81': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_81, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_82': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_82, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_83': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_83, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_84': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_84, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_85': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_85, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_86': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_86, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_87': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_87, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_88': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_88, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_89': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_89, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_90': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_90, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_91': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_91, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_92': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_92, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_93': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_93, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_94': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_94, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_95': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_95, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_96': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_96, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_97': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_97, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_98': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_98, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_99': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_99, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_100': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_100, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_101': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_101, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_102': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_102, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_103': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_103, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_104': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_104, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_105': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_105, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_106': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_106, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_107': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_107, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_108': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_108, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_109': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_109, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_110': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_110, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_111': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_111, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_112': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_112, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_113': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_113, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_114': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_114, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_115': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_115, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_116': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_116, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_117': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_117, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_118': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_118, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_119': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_119, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_120': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_120, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_121': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_121, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_122': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_122, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_123': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_123, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_124': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_124, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_125': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_125, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_126': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_126, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_127': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_127, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_128': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_128, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_129': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_129, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_130': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_130, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_131': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_131, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_132': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_132, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_133': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_133, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_134': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_134, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_135': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_135, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_136': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_136, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_137': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_137, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_138': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_138, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_139': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_139, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_140': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_140, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_141': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_141, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_142': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_142, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_143': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_143, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_144': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_144, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_145': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_145, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_146': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_146, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_147': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_147, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_148': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_148, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_149': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_149, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_150': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_150, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_151': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_151, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_152': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_152, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_153': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_153, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_154': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_154, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_155': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_155, 
        'xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_156': xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_156
    }
    
    def detect_rule_based_conflicts(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_mutants"), args, kwargs, self)
        return result 
    
    detect_rule_based_conflicts.__signature__ = _mutmut_signature(xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_orig)
    xǁConflictDetectorǁdetect_rule_based_conflicts__mutmut_orig.__name__ = 'xǁConflictDetectorǁdetect_rule_based_conflicts'
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_orig(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_1(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = None
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_2(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'XX(\d+)\s*(?:year|month|day|hour)s?\s*oldXX'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_3(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:YEAR|MONTH|DAY|HOUR)S?\s*OLD'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_4(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = None
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_5(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(None, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_6(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, None)
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_7(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_8(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, )
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_9(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.upper())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_10(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = None
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_11(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(None, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_12(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, None)
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_13(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_14(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, )
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_15(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.upper())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_16(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 or match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_17(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = None
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_18(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(None)
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_19(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(None))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_20(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(2))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_21(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = None
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_22(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(None)
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_23(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(None))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_24(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(2))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_25(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = None
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_26(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(None, match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_27(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), None, match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_28(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), None)
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_29(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_30(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_31(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), )
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_32(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.upper(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_33(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = None
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_34(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(None, match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_35(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), None, match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_36(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), None)
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_37(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_38(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_39(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), )
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_40(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.upper(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_41(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) or abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_42(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(None, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_43(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, None) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_44(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_45(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, ) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_46(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(None) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_47(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 + num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_48(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) >= 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_49(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 1:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_50(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = None
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_51(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 and MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_52(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace=None, text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_53(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=None, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_54(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=None)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_55(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_56(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_57(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, )
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_58(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="XXtempXX", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_59(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="TEMP", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_60(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=1.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_61(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = None
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_62(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 and MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_63(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace=None, text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_64(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=None, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_65(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=None)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_66(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_67(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_68(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, )
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_69(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="XXtempXX", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_70(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="TEMP", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_71(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=1.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_72(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=None,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_73(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=None,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_74(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type=None,
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_75(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=None,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_76(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=None,
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_77(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy=None
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_78(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_79(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_80(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_81(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_82(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_83(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_84(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="XXcontradictionXX",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_85(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="CONTRADICTION",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_86(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=1.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="recency"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_87(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="XXrecencyXX"
                )
        
        return None
    
    def xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_88(
        self,
        text1: str,
        text2: str,
        memory1: Optional[MemoryRecord] = None,
        memory2: Optional[MemoryRecord] = None
    ) -> Optional[Conflict]:
        """Detect numerical contradictions (e.g., age, dates)."""
        # Pattern for numbers with units
        age_pattern = r'(\d+)\s*(?:year|month|day|hour)s?\s*old'
        
        match1 = re.search(age_pattern, text1.lower())
        match2 = re.search(age_pattern, text2.lower())
        
        if match1 and match2:
            num1 = int(match1.group(1))
            num2 = int(match2.group(1))
            
            # Extract context to see if they're talking about the same thing
            context1 = self._extract_context(text1.lower(), match1.start(), match1.end())
            context2 = self._extract_context(text2.lower(), match2.start(), match2.end())
            
            if self._contexts_overlap(context1, context2) and abs(num1 - num2) > 0:
                # Use provided memories or create temporary ones
                mem1 = memory1 or MemoryRecord(namespace="temp", text=text1, importance=0.5)
                mem2 = memory2 or MemoryRecord(namespace="temp", text=text2, importance=0.5)
                
                return Conflict(
                    memory1=mem1,
                    memory2=mem2,
                    conflict_type="contradiction",
                    confidence=0.8,  # High confidence for numerical contradictions
                    evidence=f"Numerical contradiction: {num1} vs {num2}",
                    resolution_strategy="RECENCY"
                )
        
        return None
    
    xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_1': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_1, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_2': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_2, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_3': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_3, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_4': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_4, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_5': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_5, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_6': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_6, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_7': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_7, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_8': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_8, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_9': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_9, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_10': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_10, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_11': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_11, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_12': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_12, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_13': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_13, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_14': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_14, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_15': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_15, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_16': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_16, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_17': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_17, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_18': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_18, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_19': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_19, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_20': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_20, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_21': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_21, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_22': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_22, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_23': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_23, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_24': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_24, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_25': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_25, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_26': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_26, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_27': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_27, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_28': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_28, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_29': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_29, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_30': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_30, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_31': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_31, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_32': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_32, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_33': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_33, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_34': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_34, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_35': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_35, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_36': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_36, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_37': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_37, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_38': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_38, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_39': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_39, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_40': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_40, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_41': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_41, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_42': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_42, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_43': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_43, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_44': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_44, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_45': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_45, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_46': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_46, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_47': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_47, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_48': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_48, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_49': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_49, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_50': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_50, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_51': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_51, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_52': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_52, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_53': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_53, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_54': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_54, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_55': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_55, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_56': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_56, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_57': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_57, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_58': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_58, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_59': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_59, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_60': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_60, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_61': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_61, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_62': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_62, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_63': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_63, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_64': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_64, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_65': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_65, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_66': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_66, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_67': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_67, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_68': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_68, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_69': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_69, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_70': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_70, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_71': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_71, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_72': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_72, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_73': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_73, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_74': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_74, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_75': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_75, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_76': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_76, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_77': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_77, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_78': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_78, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_79': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_79, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_80': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_80, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_81': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_81, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_82': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_82, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_83': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_83, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_84': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_84, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_85': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_85, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_86': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_86, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_87': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_87, 
        'xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_88': xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_88
    }
    
    def _detect_numerical_contradiction(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _detect_numerical_contradiction.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_orig)
    xǁConflictDetectorǁ_detect_numerical_contradiction__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_detect_numerical_contradiction'
    
    def xǁConflictDetectorǁ_extract_context__mutmut_orig(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_1(self, text: str, start: int, end: int, window: int = 21) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_2(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = None
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_3(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(None, start - window)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_4(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, None)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_5(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(start - window)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_6(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, )
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_7(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(1, start - window)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_8(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start + window)
        context_end = min(len(text), end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_9(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = None
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_10(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(None, end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_11(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(len(text), None)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_12(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(end + window)
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_13(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(len(text), )
        return text[context_start:context_end]
    
    def xǁConflictDetectorǁ_extract_context__mutmut_14(self, text: str, start: int, end: int, window: int = 20) -> str:
        """Extract context around a match."""
        context_start = max(0, start - window)
        context_end = min(len(text), end - window)
        return text[context_start:context_end]
    
    xǁConflictDetectorǁ_extract_context__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_extract_context__mutmut_1': xǁConflictDetectorǁ_extract_context__mutmut_1, 
        'xǁConflictDetectorǁ_extract_context__mutmut_2': xǁConflictDetectorǁ_extract_context__mutmut_2, 
        'xǁConflictDetectorǁ_extract_context__mutmut_3': xǁConflictDetectorǁ_extract_context__mutmut_3, 
        'xǁConflictDetectorǁ_extract_context__mutmut_4': xǁConflictDetectorǁ_extract_context__mutmut_4, 
        'xǁConflictDetectorǁ_extract_context__mutmut_5': xǁConflictDetectorǁ_extract_context__mutmut_5, 
        'xǁConflictDetectorǁ_extract_context__mutmut_6': xǁConflictDetectorǁ_extract_context__mutmut_6, 
        'xǁConflictDetectorǁ_extract_context__mutmut_7': xǁConflictDetectorǁ_extract_context__mutmut_7, 
        'xǁConflictDetectorǁ_extract_context__mutmut_8': xǁConflictDetectorǁ_extract_context__mutmut_8, 
        'xǁConflictDetectorǁ_extract_context__mutmut_9': xǁConflictDetectorǁ_extract_context__mutmut_9, 
        'xǁConflictDetectorǁ_extract_context__mutmut_10': xǁConflictDetectorǁ_extract_context__mutmut_10, 
        'xǁConflictDetectorǁ_extract_context__mutmut_11': xǁConflictDetectorǁ_extract_context__mutmut_11, 
        'xǁConflictDetectorǁ_extract_context__mutmut_12': xǁConflictDetectorǁ_extract_context__mutmut_12, 
        'xǁConflictDetectorǁ_extract_context__mutmut_13': xǁConflictDetectorǁ_extract_context__mutmut_13, 
        'xǁConflictDetectorǁ_extract_context__mutmut_14': xǁConflictDetectorǁ_extract_context__mutmut_14
    }
    
    def _extract_context(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_extract_context__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_extract_context__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _extract_context.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_extract_context__mutmut_orig)
    xǁConflictDetectorǁ_extract_context__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_extract_context'
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_orig(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_1(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = None
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_2(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(None)
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_3(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = None
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_4(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(None)
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_5(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = None
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_6(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'XXtheXX', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_7(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'THE', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_8(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'XXaXX', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_9(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'A', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_10(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'XXanXX', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_11(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'AN', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_12(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'XXisXX', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_13(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'IS', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_14(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'XXareXX', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_15(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'ARE', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_16(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'XXwasXX', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_17(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'WAS', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_18(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'XXwereXX', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_19(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'WERE', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_20(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'XXbeXX', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_21(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'BE', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_22(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'XXbeenXX', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_23(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'BEEN', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_24(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'XXbeingXX'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_25(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'BEING'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_26(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = None
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_27(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 + stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_28(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = None
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_29(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 + stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_30(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = None
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_31(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 | words2
        return len(common) >= 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_32(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) > 2
    
    def xǁConflictDetectorǁ_contexts_overlap__mutmut_33(self, context1: str, context2: str) -> bool:
        """Check if two contexts are talking about the same thing."""
        # Simple heuristic: check for common words
        words1 = set(context1.split())
        words2 = set(context2.split())
        
        # Remove common stop words
        stop_words = {'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being'}
        words1 = words1 - stop_words
        words2 = words2 - stop_words
        
        # If they share at least 2 meaningful words, likely same topic
        common = words1 & words2
        return len(common) >= 3
    
    xǁConflictDetectorǁ_contexts_overlap__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_contexts_overlap__mutmut_1': xǁConflictDetectorǁ_contexts_overlap__mutmut_1, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_2': xǁConflictDetectorǁ_contexts_overlap__mutmut_2, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_3': xǁConflictDetectorǁ_contexts_overlap__mutmut_3, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_4': xǁConflictDetectorǁ_contexts_overlap__mutmut_4, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_5': xǁConflictDetectorǁ_contexts_overlap__mutmut_5, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_6': xǁConflictDetectorǁ_contexts_overlap__mutmut_6, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_7': xǁConflictDetectorǁ_contexts_overlap__mutmut_7, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_8': xǁConflictDetectorǁ_contexts_overlap__mutmut_8, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_9': xǁConflictDetectorǁ_contexts_overlap__mutmut_9, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_10': xǁConflictDetectorǁ_contexts_overlap__mutmut_10, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_11': xǁConflictDetectorǁ_contexts_overlap__mutmut_11, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_12': xǁConflictDetectorǁ_contexts_overlap__mutmut_12, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_13': xǁConflictDetectorǁ_contexts_overlap__mutmut_13, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_14': xǁConflictDetectorǁ_contexts_overlap__mutmut_14, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_15': xǁConflictDetectorǁ_contexts_overlap__mutmut_15, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_16': xǁConflictDetectorǁ_contexts_overlap__mutmut_16, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_17': xǁConflictDetectorǁ_contexts_overlap__mutmut_17, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_18': xǁConflictDetectorǁ_contexts_overlap__mutmut_18, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_19': xǁConflictDetectorǁ_contexts_overlap__mutmut_19, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_20': xǁConflictDetectorǁ_contexts_overlap__mutmut_20, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_21': xǁConflictDetectorǁ_contexts_overlap__mutmut_21, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_22': xǁConflictDetectorǁ_contexts_overlap__mutmut_22, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_23': xǁConflictDetectorǁ_contexts_overlap__mutmut_23, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_24': xǁConflictDetectorǁ_contexts_overlap__mutmut_24, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_25': xǁConflictDetectorǁ_contexts_overlap__mutmut_25, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_26': xǁConflictDetectorǁ_contexts_overlap__mutmut_26, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_27': xǁConflictDetectorǁ_contexts_overlap__mutmut_27, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_28': xǁConflictDetectorǁ_contexts_overlap__mutmut_28, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_29': xǁConflictDetectorǁ_contexts_overlap__mutmut_29, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_30': xǁConflictDetectorǁ_contexts_overlap__mutmut_30, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_31': xǁConflictDetectorǁ_contexts_overlap__mutmut_31, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_32': xǁConflictDetectorǁ_contexts_overlap__mutmut_32, 
        'xǁConflictDetectorǁ_contexts_overlap__mutmut_33': xǁConflictDetectorǁ_contexts_overlap__mutmut_33
    }
    
    def _contexts_overlap(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_contexts_overlap__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_contexts_overlap__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _contexts_overlap.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_contexts_overlap__mutmut_orig)
    xǁConflictDetectorǁ_contexts_overlap__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_contexts_overlap'
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_orig(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_1(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding or memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_2(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = None
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_3(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(None)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_4(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = None
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_5(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(None)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_6(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = None
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_7(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(None, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_8(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, None)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_9(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_10(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, )
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_11(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = None
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_12(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(None)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_13(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = None
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_14(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(None)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_15(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 or norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_16(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 >= 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_17(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 1 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_18(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 >= 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_19(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 1:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_20(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(None)
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_21(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product * (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_22(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 / norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_23(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(None)
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_24(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(None, memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_25(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, None)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_26(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory2.text)
    
    def xǁConflictDetectorǁ_calculate_similarity__mutmut_27(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> float:
        """Calculate similarity between two memories."""
        # If embeddings are available, use cosine similarity
        if memory1.embedding and memory2.embedding:
            try:
                import numpy as np
                vec1 = np.array(memory1.embedding)
                vec2 = np.array(memory2.embedding)
                
                # Cosine similarity
                dot_product = np.dot(vec1, vec2)
                norm1 = np.linalg.norm(vec1)
                norm2 = np.linalg.norm(vec2)
                
                if norm1 > 0 and norm2 > 0:
                    return float(dot_product / (norm1 * norm2))
            except Exception as e:
                logger.debug(f"Error calculating embedding similarity: {e}")
        
        # Fallback: simple text similarity
        return self._text_similarity(memory1.text, )
    
    xǁConflictDetectorǁ_calculate_similarity__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_calculate_similarity__mutmut_1': xǁConflictDetectorǁ_calculate_similarity__mutmut_1, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_2': xǁConflictDetectorǁ_calculate_similarity__mutmut_2, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_3': xǁConflictDetectorǁ_calculate_similarity__mutmut_3, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_4': xǁConflictDetectorǁ_calculate_similarity__mutmut_4, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_5': xǁConflictDetectorǁ_calculate_similarity__mutmut_5, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_6': xǁConflictDetectorǁ_calculate_similarity__mutmut_6, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_7': xǁConflictDetectorǁ_calculate_similarity__mutmut_7, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_8': xǁConflictDetectorǁ_calculate_similarity__mutmut_8, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_9': xǁConflictDetectorǁ_calculate_similarity__mutmut_9, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_10': xǁConflictDetectorǁ_calculate_similarity__mutmut_10, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_11': xǁConflictDetectorǁ_calculate_similarity__mutmut_11, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_12': xǁConflictDetectorǁ_calculate_similarity__mutmut_12, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_13': xǁConflictDetectorǁ_calculate_similarity__mutmut_13, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_14': xǁConflictDetectorǁ_calculate_similarity__mutmut_14, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_15': xǁConflictDetectorǁ_calculate_similarity__mutmut_15, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_16': xǁConflictDetectorǁ_calculate_similarity__mutmut_16, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_17': xǁConflictDetectorǁ_calculate_similarity__mutmut_17, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_18': xǁConflictDetectorǁ_calculate_similarity__mutmut_18, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_19': xǁConflictDetectorǁ_calculate_similarity__mutmut_19, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_20': xǁConflictDetectorǁ_calculate_similarity__mutmut_20, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_21': xǁConflictDetectorǁ_calculate_similarity__mutmut_21, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_22': xǁConflictDetectorǁ_calculate_similarity__mutmut_22, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_23': xǁConflictDetectorǁ_calculate_similarity__mutmut_23, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_24': xǁConflictDetectorǁ_calculate_similarity__mutmut_24, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_25': xǁConflictDetectorǁ_calculate_similarity__mutmut_25, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_26': xǁConflictDetectorǁ_calculate_similarity__mutmut_26, 
        'xǁConflictDetectorǁ_calculate_similarity__mutmut_27': xǁConflictDetectorǁ_calculate_similarity__mutmut_27
    }
    
    def _calculate_similarity(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_calculate_similarity__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_calculate_similarity__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _calculate_similarity.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_calculate_similarity__mutmut_orig)
    xǁConflictDetectorǁ_calculate_similarity__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_calculate_similarity'
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_orig(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_1(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = None
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_2(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(None)
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_3(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.upper().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_4(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = None
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_5(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(None)
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_6(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.upper().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_7(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 and not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_8(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_9(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_10(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 1.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_11(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = None
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_12(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 | words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_13(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = None
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_14(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 & words2
        
        return len(intersection) / len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_15(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) * len(union) if union else 0.0
    
    def xǁConflictDetectorǁ_text_similarity__mutmut_16(self, text1: str, text2: str) -> float:
        """Simple text similarity based on word overlap."""
        words1 = set(text1.lower().split())
        words2 = set(text2.lower().split())
        
        if not words1 or not words2:
            return 0.0
        
        intersection = words1 & words2
        union = words1 | words2
        
        return len(intersection) / len(union) if union else 1.0
    
    xǁConflictDetectorǁ_text_similarity__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_text_similarity__mutmut_1': xǁConflictDetectorǁ_text_similarity__mutmut_1, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_2': xǁConflictDetectorǁ_text_similarity__mutmut_2, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_3': xǁConflictDetectorǁ_text_similarity__mutmut_3, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_4': xǁConflictDetectorǁ_text_similarity__mutmut_4, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_5': xǁConflictDetectorǁ_text_similarity__mutmut_5, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_6': xǁConflictDetectorǁ_text_similarity__mutmut_6, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_7': xǁConflictDetectorǁ_text_similarity__mutmut_7, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_8': xǁConflictDetectorǁ_text_similarity__mutmut_8, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_9': xǁConflictDetectorǁ_text_similarity__mutmut_9, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_10': xǁConflictDetectorǁ_text_similarity__mutmut_10, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_11': xǁConflictDetectorǁ_text_similarity__mutmut_11, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_12': xǁConflictDetectorǁ_text_similarity__mutmut_12, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_13': xǁConflictDetectorǁ_text_similarity__mutmut_13, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_14': xǁConflictDetectorǁ_text_similarity__mutmut_14, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_15': xǁConflictDetectorǁ_text_similarity__mutmut_15, 
        'xǁConflictDetectorǁ_text_similarity__mutmut_16': xǁConflictDetectorǁ_text_similarity__mutmut_16
    }
    
    def _text_similarity(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_text_similarity__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_text_similarity__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _text_similarity.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_text_similarity__mutmut_orig)
    xǁConflictDetectorǁ_text_similarity__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_text_similarity'
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_orig(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_1(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = None
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_2(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(None, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_3(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, None)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_4(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_5(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, )
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_6(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = None
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_7(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'XXnotXX', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_8(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'NOT', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_9(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'XXnoXX', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_10(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'NO', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_11(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'XXneverXX', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_12(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'NEVER', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_13(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'XXnoneXX', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_14(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'NONE', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_15(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'XXhateXX', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_16(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'HATE', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_17(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'XXdislikeXX', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_18(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'DISLIKE', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_19(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'XXwrongXX', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_20(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'WRONG', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_21(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'XXincorrectXX'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_22(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'INCORRECT'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_23(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = None
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_24(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(None)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_25(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word not in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_26(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.upper() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_27(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = None
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_28(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(None)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_29(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word not in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_30(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.upper() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_31(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative == text2_has_negative:
            return 0.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_32(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 1.5  # Medium confidence
        
        return 0.0
    
    def xǁConflictDetectorǁ_check_contradiction__mutmut_33(self, text1: str, text2: str) -> float:
        """Check for contradiction between two texts."""
        # Use rule-based detection as a proxy
        conflict = self.detect_rule_based_conflicts(text1, text2)
        if conflict:
            return conflict.confidence
        
        # If no rule-based conflict, check for negative words
        negative_words = {'not', 'no', 'never', 'none', 'hate', 'dislike', 'wrong', 'incorrect'}
        text1_has_negative = any(word in text1.lower() for word in negative_words)
        text2_has_negative = any(word in text2.lower() for word in negative_words)
        
        # If one has negative and other doesn't, might be contradiction
        if text1_has_negative != text2_has_negative:
            return 0.5  # Medium confidence
        
        return 1.0
    
    xǁConflictDetectorǁ_check_contradiction__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_check_contradiction__mutmut_1': xǁConflictDetectorǁ_check_contradiction__mutmut_1, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_2': xǁConflictDetectorǁ_check_contradiction__mutmut_2, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_3': xǁConflictDetectorǁ_check_contradiction__mutmut_3, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_4': xǁConflictDetectorǁ_check_contradiction__mutmut_4, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_5': xǁConflictDetectorǁ_check_contradiction__mutmut_5, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_6': xǁConflictDetectorǁ_check_contradiction__mutmut_6, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_7': xǁConflictDetectorǁ_check_contradiction__mutmut_7, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_8': xǁConflictDetectorǁ_check_contradiction__mutmut_8, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_9': xǁConflictDetectorǁ_check_contradiction__mutmut_9, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_10': xǁConflictDetectorǁ_check_contradiction__mutmut_10, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_11': xǁConflictDetectorǁ_check_contradiction__mutmut_11, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_12': xǁConflictDetectorǁ_check_contradiction__mutmut_12, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_13': xǁConflictDetectorǁ_check_contradiction__mutmut_13, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_14': xǁConflictDetectorǁ_check_contradiction__mutmut_14, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_15': xǁConflictDetectorǁ_check_contradiction__mutmut_15, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_16': xǁConflictDetectorǁ_check_contradiction__mutmut_16, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_17': xǁConflictDetectorǁ_check_contradiction__mutmut_17, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_18': xǁConflictDetectorǁ_check_contradiction__mutmut_18, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_19': xǁConflictDetectorǁ_check_contradiction__mutmut_19, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_20': xǁConflictDetectorǁ_check_contradiction__mutmut_20, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_21': xǁConflictDetectorǁ_check_contradiction__mutmut_21, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_22': xǁConflictDetectorǁ_check_contradiction__mutmut_22, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_23': xǁConflictDetectorǁ_check_contradiction__mutmut_23, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_24': xǁConflictDetectorǁ_check_contradiction__mutmut_24, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_25': xǁConflictDetectorǁ_check_contradiction__mutmut_25, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_26': xǁConflictDetectorǁ_check_contradiction__mutmut_26, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_27': xǁConflictDetectorǁ_check_contradiction__mutmut_27, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_28': xǁConflictDetectorǁ_check_contradiction__mutmut_28, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_29': xǁConflictDetectorǁ_check_contradiction__mutmut_29, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_30': xǁConflictDetectorǁ_check_contradiction__mutmut_30, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_31': xǁConflictDetectorǁ_check_contradiction__mutmut_31, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_32': xǁConflictDetectorǁ_check_contradiction__mutmut_32, 
        'xǁConflictDetectorǁ_check_contradiction__mutmut_33': xǁConflictDetectorǁ_check_contradiction__mutmut_33
    }
    
    def _check_contradiction(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_check_contradiction__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_check_contradiction__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_contradiction.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_check_contradiction__mutmut_orig)
    xǁConflictDetectorǁ_check_contradiction__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_check_contradiction'
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_orig(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_1(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_2(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = None
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_3(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else 'XXXX'}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_4(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = None
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_5(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"XXroleXX": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_6(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"ROLE": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_7(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "XXuserXX", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_8(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "USER", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_9(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "XXcontentXX": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_10(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "CONTENT": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_11(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = None
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_12(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(None)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_13(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = None
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_14(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get(None, "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_15(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", None)
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_16(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_17(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", )
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_18(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get(None, {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_19(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", None).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_20(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get({}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_21(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", ).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_22(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get(None, [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_23(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", None)[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_24(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get([{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_25(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", )[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_26(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("XXchoicesXX", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_27(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("CHOICES", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_28(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[1].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_29(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("XXmessageXX", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_30(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("MESSAGE", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_31(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("XXcontentXX", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_32(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("CONTENT", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_33(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "XXXX")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_34(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = None
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_35(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith(None):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_36(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("XX```XX"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_37(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = None
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_38(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split(None)
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_39(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("XX\nXX")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_40(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = ""
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_41(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = ""
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_42(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(None):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_43(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith(None):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_44(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("XX```XX"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_45(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is not None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_46(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = None
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_47(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i - 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_48(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 2
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_49(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = None
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_50(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            return
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_51(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start or json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_52(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = None
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_53(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(None)
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_54(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "XX\nXX".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_55(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = None
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_56(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(None, content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_57(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', None)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_58(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_59(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', )
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_60(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'XX\{[^}]+\}XX', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_61(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = None
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_62(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(None)
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_63(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = None
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_64(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(None)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_65(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(None)
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_66(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(None, exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_67(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=None)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_68(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(exc_info=True)
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_69(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", )
            return None
    
    def xǁConflictDetectorǁanalyze_with_llm__mutmut_70(
        self,
        text1: str,
        text2: str,
        context: Optional[str] = None
    ) -> Optional[Dict[str, Any]]:
        """
        Use LLM to analyze if two statements contradict.
        
        Args:
            text1: First statement
            text2: Second statement
            context: Optional context
            
        Returns:
            Dictionary with analysis results, or None on failure
        """
        if not self.llm_client:
            return None
        
        try:
            prompt = f"""Analyze if these two statements contradict each other:

Statement 1: {text1}
Statement 2: {text2}
{f'Context: {context}' if context else ''}

Respond with a JSON object containing:
- "contradicts": boolean
- "confidence": float (0.0-1.0)
- "type": string ("contradiction", "ambiguity", or "update")
- "explanation": string

JSON response:"""
            
            messages = [
                {"role": "user", "content": prompt}
            ]
            
            response = self.llm_client.chat(messages)
            
            # Extract content from response
            content = response.get("choices", [{}])[0].get("message", {}).get("content", "")
            
            # Try to parse JSON from response
            # LLM might wrap JSON in markdown code blocks
            content = content.strip()
            if content.startswith("```"):
                # Extract JSON from code block
                lines = content.split("\n")
                json_start = None
                json_end = None
                for i, line in enumerate(lines):
                    if line.strip().startswith("```"):
                        if json_start is None:
                            json_start = i + 1
                        else:
                            json_end = i
                            break
                
                if json_start and json_end:
                    content = "\n".join(lines[json_start:json_end])
            
            # Try to find JSON object in response
            json_match = re.search(r'\{[^}]+\}', content)
            if json_match:
                result = json.loads(json_match.group())
                return result
            
            # Fallback: try parsing entire content
            try:
                result = json.loads(content)
                return result
            except json.JSONDecodeError:
                logger.warning(f"Could not parse LLM response as JSON: {content}")
                return None
        
        except Exception as e:
            logger.warning(f"Error in LLM contradiction analysis: {e}", exc_info=False)
            return None
    
    xǁConflictDetectorǁanalyze_with_llm__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁanalyze_with_llm__mutmut_1': xǁConflictDetectorǁanalyze_with_llm__mutmut_1, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_2': xǁConflictDetectorǁanalyze_with_llm__mutmut_2, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_3': xǁConflictDetectorǁanalyze_with_llm__mutmut_3, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_4': xǁConflictDetectorǁanalyze_with_llm__mutmut_4, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_5': xǁConflictDetectorǁanalyze_with_llm__mutmut_5, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_6': xǁConflictDetectorǁanalyze_with_llm__mutmut_6, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_7': xǁConflictDetectorǁanalyze_with_llm__mutmut_7, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_8': xǁConflictDetectorǁanalyze_with_llm__mutmut_8, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_9': xǁConflictDetectorǁanalyze_with_llm__mutmut_9, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_10': xǁConflictDetectorǁanalyze_with_llm__mutmut_10, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_11': xǁConflictDetectorǁanalyze_with_llm__mutmut_11, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_12': xǁConflictDetectorǁanalyze_with_llm__mutmut_12, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_13': xǁConflictDetectorǁanalyze_with_llm__mutmut_13, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_14': xǁConflictDetectorǁanalyze_with_llm__mutmut_14, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_15': xǁConflictDetectorǁanalyze_with_llm__mutmut_15, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_16': xǁConflictDetectorǁanalyze_with_llm__mutmut_16, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_17': xǁConflictDetectorǁanalyze_with_llm__mutmut_17, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_18': xǁConflictDetectorǁanalyze_with_llm__mutmut_18, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_19': xǁConflictDetectorǁanalyze_with_llm__mutmut_19, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_20': xǁConflictDetectorǁanalyze_with_llm__mutmut_20, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_21': xǁConflictDetectorǁanalyze_with_llm__mutmut_21, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_22': xǁConflictDetectorǁanalyze_with_llm__mutmut_22, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_23': xǁConflictDetectorǁanalyze_with_llm__mutmut_23, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_24': xǁConflictDetectorǁanalyze_with_llm__mutmut_24, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_25': xǁConflictDetectorǁanalyze_with_llm__mutmut_25, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_26': xǁConflictDetectorǁanalyze_with_llm__mutmut_26, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_27': xǁConflictDetectorǁanalyze_with_llm__mutmut_27, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_28': xǁConflictDetectorǁanalyze_with_llm__mutmut_28, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_29': xǁConflictDetectorǁanalyze_with_llm__mutmut_29, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_30': xǁConflictDetectorǁanalyze_with_llm__mutmut_30, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_31': xǁConflictDetectorǁanalyze_with_llm__mutmut_31, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_32': xǁConflictDetectorǁanalyze_with_llm__mutmut_32, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_33': xǁConflictDetectorǁanalyze_with_llm__mutmut_33, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_34': xǁConflictDetectorǁanalyze_with_llm__mutmut_34, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_35': xǁConflictDetectorǁanalyze_with_llm__mutmut_35, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_36': xǁConflictDetectorǁanalyze_with_llm__mutmut_36, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_37': xǁConflictDetectorǁanalyze_with_llm__mutmut_37, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_38': xǁConflictDetectorǁanalyze_with_llm__mutmut_38, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_39': xǁConflictDetectorǁanalyze_with_llm__mutmut_39, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_40': xǁConflictDetectorǁanalyze_with_llm__mutmut_40, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_41': xǁConflictDetectorǁanalyze_with_llm__mutmut_41, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_42': xǁConflictDetectorǁanalyze_with_llm__mutmut_42, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_43': xǁConflictDetectorǁanalyze_with_llm__mutmut_43, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_44': xǁConflictDetectorǁanalyze_with_llm__mutmut_44, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_45': xǁConflictDetectorǁanalyze_with_llm__mutmut_45, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_46': xǁConflictDetectorǁanalyze_with_llm__mutmut_46, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_47': xǁConflictDetectorǁanalyze_with_llm__mutmut_47, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_48': xǁConflictDetectorǁanalyze_with_llm__mutmut_48, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_49': xǁConflictDetectorǁanalyze_with_llm__mutmut_49, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_50': xǁConflictDetectorǁanalyze_with_llm__mutmut_50, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_51': xǁConflictDetectorǁanalyze_with_llm__mutmut_51, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_52': xǁConflictDetectorǁanalyze_with_llm__mutmut_52, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_53': xǁConflictDetectorǁanalyze_with_llm__mutmut_53, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_54': xǁConflictDetectorǁanalyze_with_llm__mutmut_54, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_55': xǁConflictDetectorǁanalyze_with_llm__mutmut_55, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_56': xǁConflictDetectorǁanalyze_with_llm__mutmut_56, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_57': xǁConflictDetectorǁanalyze_with_llm__mutmut_57, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_58': xǁConflictDetectorǁanalyze_with_llm__mutmut_58, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_59': xǁConflictDetectorǁanalyze_with_llm__mutmut_59, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_60': xǁConflictDetectorǁanalyze_with_llm__mutmut_60, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_61': xǁConflictDetectorǁanalyze_with_llm__mutmut_61, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_62': xǁConflictDetectorǁanalyze_with_llm__mutmut_62, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_63': xǁConflictDetectorǁanalyze_with_llm__mutmut_63, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_64': xǁConflictDetectorǁanalyze_with_llm__mutmut_64, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_65': xǁConflictDetectorǁanalyze_with_llm__mutmut_65, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_66': xǁConflictDetectorǁanalyze_with_llm__mutmut_66, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_67': xǁConflictDetectorǁanalyze_with_llm__mutmut_67, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_68': xǁConflictDetectorǁanalyze_with_llm__mutmut_68, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_69': xǁConflictDetectorǁanalyze_with_llm__mutmut_69, 
        'xǁConflictDetectorǁanalyze_with_llm__mutmut_70': xǁConflictDetectorǁanalyze_with_llm__mutmut_70
    }
    
    def analyze_with_llm(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁanalyze_with_llm__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁanalyze_with_llm__mutmut_mutants"), args, kwargs, self)
        return result 
    
    analyze_with_llm.__signature__ = _mutmut_signature(xǁConflictDetectorǁanalyze_with_llm__mutmut_orig)
    xǁConflictDetectorǁanalyze_with_llm__mutmut_orig.__name__ = 'xǁConflictDetectorǁanalyze_with_llm'
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_orig(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_1(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from or not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_2(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until or not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_3(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from or not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_4(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_5(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_6(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_7(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_8(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return False  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_9(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = None
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_10(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(None)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_11(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = None
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_12(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = False
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_13(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from and memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_14(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from or now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_15(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now <= memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_16(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = None
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_17(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = True
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_18(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until or now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_19(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now >= memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_20(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = None
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_21(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = True
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_22(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = None
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_23(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = False
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_24(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from and memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_25(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from or now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_26(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now <= memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_27(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = None
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_28(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = True
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_29(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until or now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_30(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now >= memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_31(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = None
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_32(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = True
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_33(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from or memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_34(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until or memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_35(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from or memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_36(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_37(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from and memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_38(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until <= memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_39(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until <= memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid and mem2_valid
    
    def xǁConflictDetectorǁ_check_temporal_overlap__mutmut_40(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if two memories have temporally overlapping validity periods.
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories overlap temporally, False otherwise
        """
        # If neither has temporal metadata, assume they might overlap
        if not memory1.valid_from and not memory1.valid_until and \
           not memory2.valid_from and not memory2.valid_until:
            return True  # Unknown temporal scope, assume overlap
        
        # If one has temporal metadata and other doesn't, check if current time is in range
        now = datetime.now(timezone.utc)
        
        # Check memory1 validity
        mem1_valid = True
        if memory1.valid_from or memory1.valid_until:
            if memory1.valid_from and now < memory1.valid_from:
                mem1_valid = False
            if memory1.valid_until and now > memory1.valid_until:
                mem1_valid = False
        
        # Check memory2 validity
        mem2_valid = True
        if memory2.valid_from or memory2.valid_until:
            if memory2.valid_from and now < memory2.valid_from:
                mem2_valid = False
            if memory2.valid_until and now > memory2.valid_until:
                mem2_valid = False
        
        # If both have explicit validity periods, check overlap
        if memory1.valid_from and memory1.valid_until and \
           memory2.valid_from and memory2.valid_until:
            # Check if periods overlap
            return not (memory1.valid_until < memory2.valid_from or 
                       memory2.valid_until < memory1.valid_from)
        
        # If only one has explicit period, check if current time is in both
        return mem1_valid or mem2_valid
    
    xǁConflictDetectorǁ_check_temporal_overlap__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_1': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_1, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_2': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_2, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_3': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_3, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_4': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_4, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_5': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_5, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_6': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_6, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_7': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_7, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_8': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_8, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_9': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_9, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_10': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_10, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_11': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_11, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_12': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_12, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_13': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_13, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_14': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_14, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_15': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_15, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_16': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_16, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_17': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_17, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_18': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_18, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_19': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_19, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_20': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_20, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_21': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_21, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_22': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_22, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_23': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_23, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_24': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_24, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_25': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_25, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_26': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_26, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_27': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_27, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_28': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_28, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_29': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_29, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_30': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_30, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_31': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_31, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_32': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_32, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_33': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_33, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_34': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_34, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_35': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_35, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_36': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_36, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_37': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_37, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_38': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_38, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_39': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_39, 
        'xǁConflictDetectorǁ_check_temporal_overlap__mutmut_40': xǁConflictDetectorǁ_check_temporal_overlap__mutmut_40
    }
    
    def _check_temporal_overlap(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_check_temporal_overlap__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_check_temporal_overlap__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_temporal_overlap.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_check_temporal_overlap__mutmut_orig)
    xǁConflictDetectorǁ_check_temporal_overlap__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_check_temporal_overlap'
    
    def xǁConflictDetectorǁ_is_update__mutmut_orig(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_1(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_2(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(None, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_3(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, None):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_4(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_5(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, ):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_6(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from or memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_7(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from and memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_8(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from >= memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_9(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from >= memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_10(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs(None) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_11(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at + memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_12(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) >= 86400  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_13(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86401  # More than 1 day apart
        return False
    
    def xǁConflictDetectorǁ_is_update__mutmut_14(
        self,
        memory1: MemoryRecord,
        memory2: MemoryRecord
    ) -> bool:
        """
        Check if one memory is an update of another (different time periods).
        
        Args:
            memory1: First memory
            memory2: Second memory
            
        Returns:
            True if memories appear to be updates (different periods), False otherwise
        """
        # If they don't overlap temporally and are similar, likely an update
        if not self._check_temporal_overlap(memory1, memory2):
            # Check if one is clearly after the other
            if memory1.valid_from and memory2.valid_from:
                return memory1.valid_from > memory2.valid_from or \
                       memory2.valid_from > memory1.valid_from
            # Check created_at as fallback
            return abs((memory1.created_at - memory2.created_at).total_seconds()) > 86400  # More than 1 day apart
        return True
    
    xǁConflictDetectorǁ_is_update__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_is_update__mutmut_1': xǁConflictDetectorǁ_is_update__mutmut_1, 
        'xǁConflictDetectorǁ_is_update__mutmut_2': xǁConflictDetectorǁ_is_update__mutmut_2, 
        'xǁConflictDetectorǁ_is_update__mutmut_3': xǁConflictDetectorǁ_is_update__mutmut_3, 
        'xǁConflictDetectorǁ_is_update__mutmut_4': xǁConflictDetectorǁ_is_update__mutmut_4, 
        'xǁConflictDetectorǁ_is_update__mutmut_5': xǁConflictDetectorǁ_is_update__mutmut_5, 
        'xǁConflictDetectorǁ_is_update__mutmut_6': xǁConflictDetectorǁ_is_update__mutmut_6, 
        'xǁConflictDetectorǁ_is_update__mutmut_7': xǁConflictDetectorǁ_is_update__mutmut_7, 
        'xǁConflictDetectorǁ_is_update__mutmut_8': xǁConflictDetectorǁ_is_update__mutmut_8, 
        'xǁConflictDetectorǁ_is_update__mutmut_9': xǁConflictDetectorǁ_is_update__mutmut_9, 
        'xǁConflictDetectorǁ_is_update__mutmut_10': xǁConflictDetectorǁ_is_update__mutmut_10, 
        'xǁConflictDetectorǁ_is_update__mutmut_11': xǁConflictDetectorǁ_is_update__mutmut_11, 
        'xǁConflictDetectorǁ_is_update__mutmut_12': xǁConflictDetectorǁ_is_update__mutmut_12, 
        'xǁConflictDetectorǁ_is_update__mutmut_13': xǁConflictDetectorǁ_is_update__mutmut_13, 
        'xǁConflictDetectorǁ_is_update__mutmut_14': xǁConflictDetectorǁ_is_update__mutmut_14
    }
    
    def _is_update(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_is_update__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_is_update__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _is_update.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_is_update__mutmut_orig)
    xǁConflictDetectorǁ_is_update__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_is_update'
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_orig(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_1(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id or new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_2(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id or conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_3(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id != conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_4(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = None
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_5(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id or new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_6(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id or conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_7(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id != conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_8(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = None
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_9(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = None
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_10(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = None
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_11(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(None)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_12(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at + existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_13(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(None, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_14(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, None):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_15(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_16(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, ):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_17(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = None
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_18(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "XXsame_periodXX"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_19(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "SAME_PERIOD"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_20(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type != "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_21(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "XXcontradictionXX":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_22(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "CONTRADICTION":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_23(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = None
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_24(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "XXdifferent_periodsXX"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_25(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "DIFFERENT_PERIODS"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_26(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(None, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_27(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, None):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_28(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_29(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, ):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_30(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = None
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_31(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "XXupdateXX"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_32(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "UPDATE"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_33(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = None  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_34(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(None, 0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_35(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, None)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_36(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(0.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_37(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, )  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_38(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 1.8)  # Lower confidence for updates
                conflict.evidence += " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_39(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence = " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_40(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence -= " (Different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_41(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += "XX (Different time periods suggest update rather than contradiction)XX"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_42(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (different time periods suggest update rather than contradiction)"
    
    def xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_43(
        self,
        conflict: Conflict,
        new_memory: MemoryRecord
    ) -> None:
        """
        Enhance conflict with temporal context information.
        
        Args:
            conflict: Conflict to enhance
            new_memory: New memory that triggered the conflict
        """
        # Determine which memory is the new one and which is existing
        # Compare by id if available, otherwise by object identity
        if new_memory.id and conflict.memory1.id and new_memory.id == conflict.memory1.id:
            existing_memory = conflict.memory2
        elif new_memory.id and conflict.memory2.id and new_memory.id == conflict.memory2.id:
            existing_memory = conflict.memory1
        else:
            # Fallback: assume memory1 is new (common case)
            existing_memory = conflict.memory2
        
        # Calculate temporal gap
        conflict.temporal_gap = abs(new_memory.created_at - existing_memory.created_at)
        
        # Determine temporal context
        if self._check_temporal_overlap(new_memory, existing_memory):
            conflict.temporal_context = "same_period"
            # If same period, likely a true contradiction
            if conflict.conflict_type == "contradiction":
                # Keep as contradiction
                pass
        else:
            conflict.temporal_context = "different_periods"
            # If different periods, might be an update rather than contradiction
            if self._is_update(new_memory, existing_memory):
                conflict.conflict_type = "update"
                conflict.confidence = min(conflict.confidence, 0.8)  # Lower confidence for updates
                conflict.evidence += " (DIFFERENT TIME PERIODS SUGGEST UPDATE RATHER THAN CONTRADICTION)"
    
    xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_1': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_1, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_2': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_2, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_3': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_3, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_4': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_4, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_5': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_5, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_6': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_6, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_7': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_7, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_8': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_8, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_9': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_9, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_10': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_10, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_11': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_11, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_12': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_12, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_13': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_13, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_14': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_14, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_15': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_15, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_16': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_16, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_17': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_17, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_18': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_18, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_19': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_19, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_20': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_20, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_21': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_21, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_22': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_22, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_23': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_23, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_24': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_24, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_25': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_25, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_26': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_26, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_27': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_27, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_28': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_28, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_29': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_29, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_30': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_30, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_31': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_31, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_32': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_32, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_33': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_33, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_34': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_34, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_35': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_35, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_36': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_36, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_37': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_37, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_38': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_38, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_39': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_39, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_40': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_40, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_41': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_41, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_42': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_42, 
        'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_43': xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_43
    }
    
    def _enhance_conflict_with_temporal_context(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_orig"), object.__getattribute__(self, "xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _enhance_conflict_with_temporal_context.__signature__ = _mutmut_signature(xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_orig)
    xǁConflictDetectorǁ_enhance_conflict_with_temporal_context__mutmut_orig.__name__ = 'xǁConflictDetectorǁ_enhance_conflict_with_temporal_context'

